#include "main.h"
#include <stdio.h>

/**
 * _abs - function tprints absolute value oof integer
* @c: is tge int that we use for the argument o
*
* Return: 0
*/
int _abs(int)
{
if (c > 0 || c == 0)
{
return (c);
}
else
return (c * -1);
}
