#include "main.h"
#include <stdio.h>

/**
 * _abs - function prints absolute value of integer
* @c: is tge int that we use for the argument
*
* Return: 0
*/
int _abs(int c)
{
if (c > 0 || c > 0)
{
return (c);
}
else if
{
return (c * -1);
}
else
return (0)
}
